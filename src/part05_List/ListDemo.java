package part05_List;

public class ListDemo {
    public static void main(String[] args) {

        // all the collection interface methods + new methods of List
        /* new methods of List
        * add(int index, e element)
        * addAll(int index, Collection e)
        * sort(Comparator c)
        * get(int index)
        * set(int index, Element e)
        * remove(int index)
        * indexOf(Object o)
        * lastIndexOf(Object O)
        * ListIterator<E> listIterator() (extends from Iterator(interface) )
        *   - hasNext() if more elements in collection
        *   - next() returns the next element()
        *   - remove() Removes the last element returned by iterator
        *
        *   - hasPrevious() tif there are more elements while traversing in reverse order
        *   - previous() return ths previous element
        *   - nextIndex() returns the size of the list, if it's at the end of the list
        *   - previousIndex() returns the index of the previous element
        *   - set(E e)
        *   - add(E e)
        * ListIterator<E> listIterator(int index)
        * List<E> subList(int fromIndex(inclusive), int toIndex(exclusive))
        * */
    }
}
